on:
  pull_request:
  push:
    branches:
      - master

name: Build
jobs:
  unit_test:
    name: "Run unit tests"
    runs-on: ubuntu-latest
    steps:
      - uses: docker://quintush/helm-unittest:3.3.0-0.2.2
        with:
          entrypoint: /bin/sh
          args: helm unittest --help
